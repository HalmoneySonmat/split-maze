"""SPLIT-MAZE — procgen maze RL agent + from-scratch synthetic-maze-language LM,
coupled by an artificial corpus callosum (ACC).

SPLIT-9 (negative result) -> SPLIT-MNIST (V2 reconstruction ACC verified) ->
SPLIT-MAZE (heterogeneous real environment, both networks from scratch).

See PLAN.md (v1.0) for the pre-registered design.
"""

__version__ = "0.0.1"
